'use client';

/**
 * Root Profile Page
 * Renders the Overview dashboard directly on all devices (mobile & desktop).
 */
import ProfileOverviewPage from './overview/page';

export default function ProfilePage() {
    return <ProfileOverviewPage />;
}
